{
  "planPath": "docs/superpowers/plans/2026-05-28-v0.5-implementation-plan.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 0: P0a — Android debug APK smoke on WSL/Mac",
      "status": "pending",
      "description": "**Goal:** Retire v0.4 § 8.17 deferred criterion. Build debug APK on WSL or Mac, sideload to a real Android device, confirm discovery + ≥1 ChangeEvent end-to-end.\n\n> **USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:** docs/superpowers/specs/2026-XX-XX-v0.5-android-debug-evidence.md (new), CONTRIBUTING.md, native/Cargo.toml.\n\n**Acceptance Criteria:** APK builds on WSL/Mac; installs on real Android device; discovery finds ≥1 speaker; ≥1 ChangeEvent observed; docs updated.\n\n**Verify:** `just build-apk && adb install ...` + evidence doc.\n\n```json:metadata\n{\"files\": [\"docs/superpowers/specs/2026-XX-XX-v0.5-android-debug-evidence.md\", \"CONTRIBUTING.md\", \"native/Cargo.toml\"], \"verifyCommand\": \"just build-apk && adb install app/build/app/outputs/flutter-apk/app-debug.apk\", \"acceptanceCriteria\": [\"just build-apk succeeds on WSL/Mac\", \"APK installs and launches on real Android device\", \"Discovery finds ≥1 Sonos speaker\", \"≥1 ChangeEvent observed\", \"CONTRIBUTING.md updated\", \"Cargo.toml comment updated\", \"Evidence doc captured\"], \"userGate\": true, \"tags\": [\"user-gate\"]}\n```"
    },
    {
      "id": 1,
      "subject": "Task 1: P0b — Path A vs B decision gate",
      "status": "pending",
      "description": "**Goal:** Lock event-layer foundation before topology-event work. Review event-tail.rs traces vs v0.4 spike Path B numbers. 1-page decision doc.\n\n> **USER-ORDERED GATE — NON-SKIPPABLE.** Close only after every acceptanceCriteria item has been re-validated independently with output captured.\n\n**Files:** docs/superpowers/specs/2026-XX-XX-v0.5-path-revisit.md (new), docs/ROADMAP.md.\n\n**Acceptance Criteria:** ≥30min idle + ≥30min active traces against current main; decision doc cites concrete evidence; decision unambiguous; ROADMAP updated.\n\n**Verify:** Decision doc exists; ROADMAP reflects state.\n\n```json:metadata\n{\"files\": [\"docs/superpowers/specs/2026-XX-XX-v0.5-path-revisit.md\", \"docs/ROADMAP.md\"], \"verifyCommand\": \"test -f docs/superpowers/specs/2026-*-v0.5-path-revisit.md\", \"acceptanceCriteria\": [\"≥30 min idle + ≥30 min active traces captured\", \"Decision doc references concrete evidence\", \"Decision stated unambiguously\", \"ROADMAP updated\"], \"userGate\": true, \"tags\": [\"user-gate\"]}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: P0c — ZGT subscription probe spike",
      "status": "pending",
      "description": "**Goal:** Confirm SDK reactive layer fires NOTIFYs on ZoneGroupTopology subscription during regrouping. Gate for S1 Option-3 viability.\n\n> **USER-ORDERED GATE — NON-SKIPPABLE.** Close only after every acceptanceCriteria item has been re-validated independently with output captured.\n\n**Files:** native/crates/wire/tests/live_topology_probe.rs (new, live-tests, #[ignore]), docs/sonos-notes.md.\n\n**Acceptance Criteria:** Test compiles; NOTIFYs observed for ≥2 regroup ops; per-speaker vs per-household characterized; payload shape captured; sonos-notes appended.\n\n**Verify:** `cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only topology_probe -- --nocapture`\n\n```json:metadata\n{\"files\": [\"native/crates/wire/tests/live_topology_probe.rs\", \"docs/sonos-notes.md\"], \"verifyCommand\": \"cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only topology_probe -- --nocapture\", \"acceptanceCriteria\": [\"Probe test compiles\", \"NOTIFYs observed for ≥2 regroup operations\", \"Per-speaker vs per-household model characterized\", \"Payload shape captured\", \"sonos-notes updated\"], \"userGate\": true, \"tags\": [\"user-gate\"]}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: S1.1 — Wire surface + ChangeEvent::TopologyChanged + MockWire impls",
      "status": "pending",
      "blockedBy": [2],
      "description": "**Goal:** LAN-free skeleton. Wire trait grows subscribe_topology + refresh_topology; ChangeEvent gains TopologyChanged; MockWire fully impls + test seams; SonosWire skeleton compiles; FRB DTO + map updated.\n\n**Files:** native/crates/core/src/wire.rs, native/crates/core/src/events.rs, native/crates/mock/src/lib.rs, native/crates/wire/src/adapter.rs, native/src/api.rs, native/src/map.rs.\n\n**Acceptance Criteria:** Trait methods added; ChangeEvent::TopologyChanged exists; ChangeEventDto::TopologyChanged + match; MockWire impls + seams; SonosWire skeleton compiles; subscribe_topology idempotent; just check + test green.\n\n**Verify:** `just gen && just check && just test`\n\n```json:metadata\n{\"files\": [\"native/crates/core/src/wire.rs\", \"native/crates/core/src/events.rs\", \"native/crates/mock/src/lib.rs\", \"native/crates/wire/src/adapter.rs\", \"native/src/api.rs\", \"native/src/map.rs\"], \"verifyCommand\": \"just gen && just check && just test\", \"acceptanceCriteria\": [\"Wire trait has subscribe_topology + refresh_topology\", \"ChangeEvent::TopologyChanged exists\", \"ChangeEventDto::TopologyChanged + map covered\", \"MockWire impls + test seams in place\", \"SonosWire compiles with skeleton impls\", \"subscribe_topology idempotent\", \"just check && just test green\"]}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: S1.2 — SonosWire production impl",
      "status": "pending",
      "blockedBy": [3],
      "description": "**Goal:** Real SDK ZGT subscription in EventPump; emit ChangeEvent::TopologyChanged on NOTIFY. refresh_topology as GetZoneGroupState SOAP against cached IP.\n\n**Files:** native/crates/wire/src/events.rs, native/crates/wire/src/adapter.rs, native/crates/wire/tests/live_topology_events.rs (new, live-tests).\n\n**Acceptance Criteria:** subscribe_topology activates SDK ZGT watch; operator regroup → TopologyChanged within ~5s; refresh_topology issues GetZoneGroupState (no SSDP); returns fresh DiscoverySnapshot; Network error leaves caches unchanged; live test passes.\n\n**Verify:** `just check && just test && cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only live_topology_events`\n\n```json:metadata\n{\"files\": [\"native/crates/wire/src/events.rs\", \"native/crates/wire/src/adapter.rs\", \"native/crates/wire/tests/live_topology_events.rs\"], \"verifyCommand\": \"just check && just test && cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only live_topology_events\", \"acceptanceCriteria\": [\"subscribe_topology activates SDK ZGT watch\", \"Operator regroup → TopologyChanged within ~5s\", \"refresh_topology issues GetZoneGroupState SOAP, no SSDP\", \"refresh_topology returns fresh DiscoverySnapshot\", \"refresh_topology Network error leaves caches unchanged\", \"just check && just test green\", \"Live topology test passes\"]}\n```"
    },
    {
      "id": 5,
      "subject": "Task 5: S1.3 — oto-app cache path + FRB consumer + Dart provider",
      "status": "pending",
      "blockedBy": [4],
      "description": "**Goal:** Wire full topology refresh path through oto-app + FRB consumer + Dart TopologyController (250ms debounce).\n\n**Files:** native/crates/app/src/lib.rs, native/crates/app/src/state_manager.rs, native/src/api.rs, app/lib/src/state/topology.dart (new).\n\n**Acceptance Criteria:** oto_app::refresh_topology() locks DISCOVER_LOCK + bumps gen + clears group-keyed caches; discover_with auto-invokes subscribe_topology; FRB cmd exposed; consumer pass-through for TopologyChanged; Dart debouncer 250ms; per-speaker caches survive; group-keyed cleared; tests green.\n\n**Verify:** `just gen && just check && just test && (cd app && flutter test)`\n\n```json:metadata\n{\"files\": [\"native/crates/app/src/lib.rs\", \"native/crates/app/src/state_manager.rs\", \"native/src/api.rs\", \"app/lib/src/state/topology.dart\"], \"verifyCommand\": \"just gen && just check && just test && (cd app && flutter test)\", \"acceptanceCriteria\": [\"oto_app::refresh_topology() locks DISCOVER_LOCK, bumps gen, clears group-keyed caches\", \"discover_with calls subscribe_topology before subscribe_speakers\", \"FRB refresh_topology cmd exposed\", \"FRB consumer forwards TopologyChanged with no cache mutation\", \"Dart TopologyController debounces 250ms and re-pulls\", \"Per-speaker caches survive refresh; group-keyed caches cleared\", \"Unit + integration tests green\"]}\n```"
    },
    {
      "id": 6,
      "subject": "Task 6: S2 — SubscriptionError reactive emission",
      "status": "pending",
      "description": "**Goal:** Emit ChangeEvent::SubscriptionError/Recovered on command-dispatch transitions (Approach A). Per-speaker Healthy↔Errored state in oto-app.\n\n**Files:** native/crates/app/src/lib.rs, native/crates/app/src/health.rs (new), native/crates/app/src/events.rs (new — app-side channel), native/src/api.rs, docs/ROADMAP.md.\n\n**Acceptance Criteria:** Health state tracked; Network error from Healthy → SubscriptionError; Ok from Errored → SubscriptionRecovered; Backend/NotFound do not change; edge-triggered; resets on discover_with; speaker_state cache read does not trigger; ROADMAP records B + C as post-1.0 follow-ups.\n\n**Verify:** `just check && just test`\n\n```json:metadata\n{\"files\": [\"native/crates/app/src/lib.rs\", \"native/crates/app/src/health.rs\", \"native/crates/app/src/events.rs\", \"native/src/api.rs\", \"docs/ROADMAP.md\"], \"verifyCommand\": \"just check && just test\", \"acceptanceCriteria\": [\"Per-speaker HealthState tracked\", \"Network error from Healthy → SubscriptionError + Errored\", \"Ok from Errored → SubscriptionRecovered + Healthy\", \"Backend/NotFound errors do not change health\", \"Edge-triggered (no duplicates)\", \"Reset on wire replacement\", \"speaker_state cache read does not trigger health check\", \"ROADMAP follow-ups recorded\"]}\n```"
    },
    {
      "id": 7,
      "subject": "Task 7: S3 — Android MulticastLock",
      "status": "pending",
      "blockedBy": [0],
      "description": "**Goal:** Make Android release-build discovery work. Acquire WifiManager.MulticastLock around discover() SSDP window. Kotlin handler + Dart shim + provider wrapping.\n\n**Files:** app/android/app/src/main/kotlin/me/oszkar/oto/MulticastLockHandler.kt (new), MainActivity.kt, app/lib/src/platform/android_multicast_lock.dart (new), app/lib/src/state/discovery.dart, AndroidManifest.xml, native/src/api.rs.\n\n**Acceptance Criteria:** Release APK on real Android device finds speakers; acquire before discover SSDP; release after (success or error); reference-counted; refresh_topology does NOT acquire; non-Android short-circuit; TODO(v0.5) comments deleted.\n\n**Verify:** `flutter build apk --release && adb install -r app/build/app/outputs/flutter-apk/app-release.apk` on real Android device.\n\n```json:metadata\n{\"files\": [\"app/android/app/src/main/kotlin/me/oszkar/oto/MulticastLockHandler.kt\", \"app/android/app/src/main/kotlin/me/oszkar/oto/MainActivity.kt\", \"app/lib/src/platform/android_multicast_lock.dart\", \"app/lib/src/state/discovery.dart\", \"app/android/app/src/main/AndroidManifest.xml\", \"native/src/api.rs\"], \"verifyCommand\": \"just check && (cd app && flutter test && flutter build apk --release)\", \"acceptanceCriteria\": [\"Release APK discovers Sonos household on real Android device\", \"Lock acquire called before discover SSDP phase\", \"Lock release called after discover returns (success or error)\", \"refresh_topology does NOT acquire lock\", \"Non-Android platforms unaffected\", \"TODO(v0.5) comments deleted\"]}\n```"
    },
    {
      "id": 8,
      "subject": "Task 8: S4 — Model repopulate",
      "status": "pending",
      "description": "**Goal:** Populate SpeakerIdentity.model from device_description.xml via parallel ureq fetches inside discover() AND refresh_topology(). ~50-200ms latency, no caching, honest-partial on timeout.\n\n**Files:** native/crates/wire/src/device_description.rs (new), native/crates/wire/src/adapter.rs, native/crates/wire/src/lib.rs, native/crates/wire/tests/live_model_populate.rs (new, live-tests).\n\n**Acceptance Criteria:** Both speakers have model=Some after discover; refresh_topology also populates; per-speaker 1s connect + 2s read timeout; failures → model=None without discover failing; parallel; no caching; unit test for parser; live test passes.\n\n**Verify:** `just check && just test && cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only model_populated_after_discover`\n\n```json:metadata\n{\"files\": [\"native/crates/wire/src/device_description.rs\", \"native/crates/wire/src/adapter.rs\", \"native/crates/wire/src/lib.rs\", \"native/crates/wire/tests/live_model_populate.rs\"], \"verifyCommand\": \"just check && just test && cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only model_populated_after_discover\", \"acceptanceCriteria\": [\"After discover, both speakers have model=Some(...)\", \"After refresh_topology, model populated\", \"Per-speaker timeout 1s+2s; failure → model=None without discover failing\", \"Parallel fetches\", \"No caching\", \"just check && just test green\"]}\n```"
    },
    {
      "id": 9,
      "subject": "Task 9: v0.5 dogfood + acceptance + chore(release): v0.5.0",
      "status": "pending",
      "blockedBy": [0, 1, 2, 3, 4, 5, 6, 7, 8],
      "description": "**Goal:** Capture v0.5 hardware acceptance evidence; record lock-hold histograms (S5); sweep TODO(v0.5) markers; CHANGELOG + version bumps + doc sync; release PR.\n\n> **USER-ORDERED GATE — NON-SKIPPABLE.** Close only after every acceptanceCriteria item has been re-validated independently with output captured.\n\n**Files:** docs/superpowers/specs/2026-XX-XX-v0.5-release-evidence/ (new dir + logs + README), native/crates/app/src/lib.rs, native/examples/event-tail.rs, CHANGELOG.md, app/pubspec.yaml, native/Cargo.toml, docs/ARCHITECTURE.md, docs/ROADMAP.md, README.md, native/crates/wire/src/control.rs, native/deny.toml, native/src/api.rs.\n\n**Acceptance Criteria:** ≥30min idle + ≥30min active dogfood with regroup; v0.4 acceptance criteria still pass; S1 hardware (TopologyChanged ~5s); S3 hardware (release APK Android discovers); S4 hardware (model populated); lock-hold histograms; S5 evaluated; TODO(v0.5) all resolved/re-tagged; CHANGELOG entry; versions bumped; doc sync; PR opened.\n\n**Verify:** Manual review of evidence dir + `just check && just test` green.\n\n```json:metadata\n{\"files\": [\"docs/superpowers/specs/2026-XX-XX-v0.5-release-evidence/\", \"native/crates/app/src/lib.rs\", \"native/examples/event-tail.rs\", \"CHANGELOG.md\", \"app/pubspec.yaml\", \"native/Cargo.toml\", \"docs/ARCHITECTURE.md\", \"docs/ROADMAP.md\", \"README.md\", \"native/crates/wire/src/control.rs\", \"native/deny.toml\", \"native/src/api.rs\"], \"verifyCommand\": \"just check && just test\", \"acceptanceCriteria\": [\"≥30 min idle + ≥30 min active dogfood with topology events\", \"All v0.4 acceptance criteria still pass\", \"S1 hardware: TopologyChanged ~5s after regroup\", \"S3 hardware: release APK on Android device discovers\", \"S4 hardware: model populated for ≥2 speakers\", \"Lock-hold histograms recorded\", \"S5 evaluated\", \"All TODO(v0.5) markers resolved or re-tagged\", \"CHANGELOG v0.5.0 entry\", \"Version 0.5.0 in pubspec.yaml + Cargo.toml\", \"Doc sync complete\", \"PR opened\"], \"userGate\": true, \"requireEvidenceTokens\": [[\"v0.4-baseline\", \"pre-v0.5\", \"unchanged\"], [\"v0.5-additions\", \"topology-events\", \"S3-MulticastLock\", \"S4-model\"]], \"tags\": [\"user-gate\"]}\n```"
    }
  ],
  "lastUpdated": "2026-05-28T00:00:00Z"
}
