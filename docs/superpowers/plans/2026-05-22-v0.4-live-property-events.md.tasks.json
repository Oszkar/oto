{
  "planPath": "docs/superpowers/plans/2026-05-22-v0.4-live-property-events.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Slice 1 — v0.4 end-to-end skeleton (LAN-free)",
      "status": "completed",
      "description": "Validate the full v0.4 architecture against MockWire — ChangeEvent enum, Wire trait additions, StateManager skeleton, subscribe_change_events FRB fn, Dart StreamProvider, integration test — without any Path A code.\n\nFull task body in `docs/superpowers/plans/2026-05-22-v0.4-live-property-events.md` § Task 1 and in the task system (TaskGet #1).\n\n```json:metadata\n{\"files\": [\"native/crates/core/src/events.rs\", \"native/crates/core/src/wire.rs\", \"native/crates/core/src/lib.rs\", \"native/crates/mock/src/lib.rs\", \"native/crates/app/src/state_manager.rs\", \"native/crates/app/src/lib.rs\", \"native/src/api.rs\", \"native/src/map.rs\", \"app/lib/src/state/events.dart\", \"app/integration_test/v0_4_events_test.dart\"], \"verifyCommand\": \"just check && just test && (cd app && flutter test integration_test/v0_4_events_test.dart -d windows)\", \"acceptanceCriteria\": [\"DEV-ONLY FRB pre-check stubs deleted in first commit\", \"ChangeEvent::Volume + SubscriptionError + SubscriptionRecovered in oto-core\", \"Wire::subscribe_speakers + take_event_stream on trait, MockWire implements both\", \"oto-app::discover_with auto-invokes subscribe_speakers\", \"MockWire seeds Volume per speaker on subscribe + auto-emits on set_volume + push_event affordance\", \"StateManager applies ChangeEvent::Volume to per-speaker RwLock cache\", \"subscribe_change_events FRB fn runs consumer loop on FRB worker; cancel via sink.add().is_err()\", \"subscribeChangeEventsProvider depends on discoveryProvider\", \"Integration test passes against MockWire: seeds + mutation + push_event scenarios\", \"just check + just test green on Windows\"]}\n```"
    },
    {
      "id": 2,
      "subject": "Slice 2 — v0.4 fill data model (Mute/Playback/Track + per-group transport)",
      "status": "completed",
      "blockedBy": [1],
      "description": "Extend ChangeEvent, StateManager, MockWire, and the FRB DTO with all four watched properties. After this PR, the LAN-free integration test exercises Volume/Mute (per-speaker) + Playback/Track (per-group with coordinator addressing) end-to-end.\n\nFull task body in `docs/superpowers/plans/2026-05-22-v0.4-live-property-events.md` § Task 2 and in the task system (TaskGet #2).\n\n```json:metadata\n{\"files\": [\"native/crates/core/src/events.rs\", \"native/crates/app/src/state_manager.rs\", \"native/crates/app/src/lib.rs\", \"native/crates/mock/src/lib.rs\", \"native/src/api.rs\", \"native/src/map.rs\", \"app/integration_test/v0_4_events_test.dart\"], \"verifyCommand\": \"just check && just test && (cd app && flutter test integration_test/v0_4_events_test.dart -d windows)\", \"acceptanceCriteria\": [\"ChangeEvent has 6 v0.4 variants (Volume/Mute/Playback/Track/SubscriptionError/SubscriptionRecovered)\", \"StateManager has per-speaker (volume, muted) + per-group (transport, track) caches\", \"MockWire: set_mute/play/pause auto-emit; next/previous no-op; seed emits per-property per-speaker + per-group\", \"AVTransport events carry GroupId, not coordinator's SpeakerId\", \"Integration test grouped scenario: mock.play(group) emits exactly one Playback with the group's GroupId\"]}\n```"
    },
    {
      "id": 3,
      "subject": "Slice 3 — v0.4 Path A production wire (oto-wire pump thread + sonos-sdk-state)",
      "status": "in_progress",
      "blockedBy": [2],
      "description": "Plug real Sonos events into the proven architecture. Wires sonos-sdk-state + sonos-event-manager into oto-wire; pump thread maps upstream watches to ChangeEvent; AVTransport coordinator-only filter; in-band SubscriptionError/Recovered.\n\nFull task body in `docs/superpowers/plans/2026-05-22-v0.4-live-property-events.md` § Task 3 and in the task system (TaskGet #3).\n\n```json:metadata\n{\"files\": [\"native/Cargo.toml\", \"native/crates/wire/Cargo.toml\", \"native/crates/wire/src/events.rs\", \"native/crates/wire/src/lib.rs\", \"native/crates/wire/src/control.rs\", \"native/crates/wire/tests/live_events.rs\"], \"verifyCommand\": \"just check && just test && cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only\", \"acceptanceCriteria\": [\"sonos-sdk-state family pinned exact at spike-tested versions; cargo deny passes\", \"oto-wire::events module owns one OS pump thread; joins on SonosWire drop\", \"per-speaker Volume+Mute + per-coordinator PlaybackState+CurrentTrack watches; Position NOT watched\", \"AVTransport events filtered to coordinator-only via discover-built speaker_to_coordinator map\", \"ChangeEvent per-speaker carries SpeakerId; per-group carries GroupId\", \"SubscriptionError/Recovered emit on observable upstream failure/recovery\", \"hardware live tests pass on 4-speaker LAN\"]}\n```"
    },
    {
      "id": 4,
      "subject": "Slice 4 — v0.4 speaker_state cache-read swap",
      "status": "pending",
      "blockedBy": [3],
      "description": "Production oto_app::speaker_state reads StateManager directly, no longer dispatching through Wire::speaker_state. The trait method stays for the v0.3 contract + mock-based tests.\n\nFull task body in `docs/superpowers/plans/2026-05-22-v0.4-live-property-events.md` § Task 4 and in the task system (TaskGet #4).\n\n```json:metadata\n{\"files\": [\"native/crates/app/src/lib.rs\", \"native/crates/app/src/state_manager.rs\", \"native/src/api.rs\"], \"verifyCommand\": \"just check && just test && (cd app && flutter test integration_test/v0_4_events_test.dart -d windows)\", \"acceptanceCriteria\": [\"oto_app::speaker_state reads StateManager, not Wire::speaker_state\", \"honest partial: Option::None for fields not yet event-fed\", \"transport resolves via speaker→coordinator→group\", \"SpeakerStateDto external shape unchanged from v0.3\", \"existing slot-lifecycle test passes via event-driven state in MockWire\", \"Wire::speaker_state trait remains for mock tests\"]}\n```"
    },
    {
      "id": 5,
      "subject": "Slice 5 — v0.4 dogfood + hardware acceptance + doc sync (release gate)",
      "status": "pending",
      "blockedBy": [4],
      "description": "USER-ORDERED GATE — NON-SKIPPABLE. Ship v0.4. Dogfood binary, 17-criteria hardware acceptance, doc sync, lock-granularity audit comment.\n\nFull task body in `docs/superpowers/plans/2026-05-22-v0.4-live-property-events.md` § Task 5 and in the task system (TaskGet #5).\n\n```json:metadata\n{\"files\": [\"native/examples/event-tail.rs\", \"native/crates/app/src/lib.rs\", \"docs/ARCHITECTURE.md\", \"docs/ROADMAP.md\", \"docs/sonos-notes.md\", \"CHANGELOG.md\", \"README.md\", \"app/pubspec.yaml\", \"native/Cargo.toml\"], \"verifyCommand\": \"just check && just test && cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only && cargo run --example event-tail\", \"acceptanceCriteria\": [\"event-tail.rs builds + runs against LAN\", \"event-tail.rs ≥30 min idle without growth/drops/spurious\", \"event-tail.rs ≥30 min active with music + manual volume changes\", \"≥1 GENA renewal cycle observed ~25 min mark\", \"all 17 criteria from spec § 8 pass with captured artifacts\", \"Android debug APK smoke-tests events on hardware\", \"ARCHITECTURE+ROADMAP+sonos-notes+CHANGELOG+README all synced\", \"lock-granularity audit comment in oto-app module-level doc\", \"version bumped to 0.4.0 per RELEASING.md\"], \"userGate\": true, \"tags\": [\"user-gate\"]}\n```"
    }
  ],
  "lastUpdated": "2026-05-23T10:00:00Z"
}
