{
  "planPath": "docs/superpowers/plans/2026-06-04-v0.5.1-group-operations.md",
  "specPath": "docs/superpowers/specs/2026-06-04-v0.5.1-group-operations-design.md",
  "branch": "docs/v0.5.1-group-operations",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: Spike — mutating group SOAP + Option D feasibility",
      "status": "completed",
      "description": "**Goal:** On the 4-speaker LAN, prove form/break + group-volume SOAP and choose Option D's approach before any production code. USER-ORDERED GATE — non-skippable (user said 'spike-first').\n\n**Verify:** `cargo run -p oto-wire --example group_ops_probe --features live-tests` on the LAN; findings appended to sonos-notes.md.\n\n```json:metadata\n{\"userGate\": true, \"tags\": [\"user-gate\"], \"requiresUserSpecification\": false, \"files\": [\"native/crates/wire/examples/group_ops_probe.rs\", \"docs/sonos-notes.md\"], \"verifyCommand\": \"cargo run -p oto-wire --example group_ops_probe --features live-tests\", \"acceptanceCriteria\": [\"x-rincon join makes joiner a group member; GroupId captured\", \"BecomeCoordinatorOfStandaloneGroup splits member out; coordinator-leave recorded\", \"SetGroupVolume/Relative/Mute confirmed + clamp + GetGroupVolume\", \"GroupVolume/GroupMute watch emits seed+change NOTIFYs; cadence/dedup captured\", \"Option D approach chosen with reason\", \"findings appended to sonos-notes.md; go/no-go recorded\"]}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Form/break — join_group / leave_group",
      "status": "completed",
      "blockedBy": [1],
      "description": "**Goal:** Additive form/break across all layers; Dart facade is await-only (refresh via the topology-event path, no self-trigger — an immediate self-refresh races the topology settle).\n\n**Verify:** `just check && just test`, then `cargo nextest run -p oto-wire --features live-tests --run-ignored ignored-only`.\n\n```json:metadata\n{\"files\": [\"native/crates/core/src/wire.rs\", \"native/crates/wire/src/adapter.rs\", \"native/crates/wire/src/grouping.rs\", \"native/crates/mock/src/lib.rs\", \"native/crates/app/src/lib.rs\", \"native/src/api.rs\", \"native/src/map.rs\", \"app/lib/src/state/grouping.dart\", \"native/crates/wire/tests/live_grouping.rs\"], \"verifyCommand\": \"just check && just test\", \"acceptanceCriteria\": [\"join_group/leave_group on trait + all 4 impls\", \"MockWire join/leave mutate topology; unknown id -> NotFound\", \"FRB shims route via oto-app with health observe\", \"Dart GroupingCommands await-only - NO self-trigger; debounced TopologyController drives refresh\", \"just gen clean; just check + just test green\", \"hardware join_group -> topology reflects group\"]}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: Group volume/mute — commands + read/event",
      "status": "completed",
      "blockedBy": [1, 2],
      "description": "**Goal:** set_group_volume/set_group_mute commands + GroupVolume/GroupMute read+event on the existing single FRB stream.\n\n**Verify:** `just check && just test`; live command+event round-trip.\n\n```json:metadata\n{\"files\": [\"native/crates/core/src/events.rs\", \"native/crates/core/src/wire.rs\", \"native/crates/wire/src/grouping.rs\", \"native/crates/wire/src/adapter.rs\", \"native/crates/wire/src/events.rs\", \"native/crates/app/src/lib.rs\", \"native/crates/app/src/state_manager.rs\", \"native/src/api.rs\", \"native/src/map.rs\", \"app/lib/src/state/playback.dart\", \"native/crates/wire/tests/live_grouping.rs\"], \"verifyCommand\": \"just check && just test\", \"acceptanceCriteria\": [\"set_group_volume/set_group_mute on all 4 impls coordinator-routed\", \"ChangeEvent + ChangeEventDto GroupVolume/GroupMute; map.rs maps them\", \"pump watches GroupVolume/GroupMute on coordinators; TopologyFilter dirty-drops them\", \"StateManager per-GroupId group cache + apply + readers\", \"MockWire set_group_* auto-emit events\", \"just check + just test green; hardware command+event round-trip\"]}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: Option D — topology fast-path (pump rebuild on refresh)",
      "status": "completed",
      "blockedBy": [1, 3],
      "description": "**Goal:** refresh_topology() rebuilds pump routing (spike-chosen approach) so group events route to the new coordinator; full re-discover stays as fallback. ESCAPE HATCH: slip to v0.5.2 if hard.\n\n**Verify:** `just check && just test`; live regroup->fast-refresh->Playback routes (no drop).\n\n```json:metadata\n{\"files\": [\"native/crates/wire/src/adapter.rs\", \"native/crates/wire/src/events.rs\", \"native/crates/app/src/lib.rs\", \"native/src/api.rs\", \"app/lib/src/state/topology.dart\", \"app/lib/src/state/grouping.dart\", \"native/crates/wire/tests/live_grouping.rs\"], \"verifyCommand\": \"just check && just test\", \"acceptanceCriteria\": [\"refresh_topology rebuilds pump routing; group events route to new coordinator no-drop\", \"refresh_topology error falls back to full re-discover\", \"Dart TopologyController + GroupingCommands use refresh_topology with fallback\", \"just check + just test green; hardware regroup->fast-refresh->Playback routes\"]}\n```"
    },
    {
      "id": 5,
      "subject": "Task 5: Docs sweep — ARCHITECTURE / ROADMAP / sonos-notes / CHANGELOG",
      "status": "pending",
      "blockedBy": [2, 3, 4],
      "description": "**Goal:** Docs match the shipped v0.5.1 surface; retire the milestone spec + plan post-release.\n\n**Verify:** `just check`; manual read-through; git grep for stale 'deferred to v0.5.1'.\n\n```json:metadata\n{\"files\": [\"docs/ARCHITECTURE.md\", \"docs/ROADMAP.md\", \"docs/sonos-notes.md\", \"CHANGELOG.md\"], \"verifyCommand\": \"just check\", \"acceptanceCriteria\": [\"ARCHITECTURE Wire block + event path + refresh updated\", \"ROADMAP v0.5.1 status + deferred items resolved\", \"CHANGELOG [0.5.1] jargon-free entry + compare links\", \"internal links resolve; milestone spec+plan retired\"]}\n```"
    }
  ],
  "lastUpdated": "2026-06-04T12:00:00Z"
}
