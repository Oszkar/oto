# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# Rust
target/
Cargo.lock.bak
**/*.rs.bk

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.log
# Spike evidence logs are intentionally checked in — they're durable
# protocol observations, not transient build output. Override *.log.
!docs/superpowers/specs/**/*.log

# Coverage
coverage/
lcov.info

# Local env
.env
.env.local

# Claude Code per-user settings (the shared `.claude/settings.json` may be
# committed if/when one is added; the `.local.json` overlay is per-developer).
.claude/settings.local.json
