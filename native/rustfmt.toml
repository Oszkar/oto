edition = "2024"
max_width = 100
use_small_heuristics = "Default"
reorder_imports = true
reorder_modules = true
newline_style = "Unix"
# `imports_granularity = "Crate"` and other nightly-only options are not set
# here because we pin rustup to stable in `rust-toolchain.toml`.
